"""
Paquete "indices": todo lo relacionado con índices de ajuste de alquileres.

Este archivo es lo que convierte a la carpeta "indices/" en un PAQUETE.
Puede quedar vacío (es lo más común) o puede re-exportar cosas para que
quien use el paquete no necesite saber en qué módulo interno viven.

Módulos:
  - bcra.py       → hablar con la API del Banco Central
  - contratos.py  → cálculos sobre contratos de alquiler

Ejercicio 6 (opcional, al final de todo): re-exportá acá las funciones
principales para poder escribir `from indices import aplicar_ajuste_icl`
en vez de `from indices.contratos import aplicar_ajuste_icl`.
"""
